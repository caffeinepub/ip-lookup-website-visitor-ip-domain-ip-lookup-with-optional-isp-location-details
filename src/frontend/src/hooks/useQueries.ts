// This file is intentionally empty as all backend queries are handled by useVisitorIp and useIpLookup
// The backend actor is not used for this IP lookup application since all data comes from external APIs
export {};
